import React from "react";
import styled from "styled-components";

export const StyledFaceRecognition = styled.div``;

interface PropType {}

export const FaceRecognitionPage = ({}: PropType): JSX.Element | null => {
  return <StyledFaceRecognition></StyledFaceRecognition>;
};
