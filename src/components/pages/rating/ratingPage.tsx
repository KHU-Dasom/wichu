import React from "react";
import styled from "styled-components";

export const StyledRatingPage = styled.div``;

interface PropType {}

export const RatingPage = ({}: PropType): JSX.Element | null => {
  return <StyledRatingPage></StyledRatingPage>;
};
