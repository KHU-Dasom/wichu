import React from "react";
import styled from "styled-components";

export const StyledSignUpPage = styled.div``;

interface PropType {}

export const SignUpPage = ({}: PropType): JSX.Element | null => {
  return <StyledSignUpPage></StyledSignUpPage>;
};
