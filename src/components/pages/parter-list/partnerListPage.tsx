import React from "react";
import styled from "styled-components";

export const StyledPartnerListPage = styled.div``;

interface PropType {}

export const PartnerListPage = ({}: PropType): JSX.Element | null => {
  return <StyledPartnerListPage></StyledPartnerListPage>;
};
