import React from "react";
import styled from "styled-components";

export const StyledSignInPage = styled.div``;

interface PropType {}

export const SignInPage = ({}: PropType): JSX.Element | null => {
  return <StyledSignInPage></StyledSignInPage>;
};
